version: "3.8"

services:

  backend:

    build:
      context: ./backend

    container_name: task-collaboration-backend

    ports:
      - "3000:3000"

    env_file:
      - ./backend/.env

    restart: unless-stopped